One-line: in-page view switcher — hairline rule with a 2px teal indicator under the active tab.

```jsx
<Tabs items={[{value:"overview",label:"Übersicht"},{value:"logs",label:"Logs",count:12}]} onChange={setView} />
```

Sits directly under the page header, full content width. No pill tabs, no boxed tabs.
