One-line: icon-only action for dense surfaces — always pass `label`.

```jsx
<IconButton icon="settings" label="Einstellungen" />
```

Variants: ghost (default, inside rows and headers), outline (standalone, e.g. next to a search field).
