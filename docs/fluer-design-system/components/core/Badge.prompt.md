One-line: status marker for deploy/build/environment state; tinted background + hairline border, 3px radius (never a pill).

```jsx
<Badge tone="success" dot>Live</Badge>
```

Tones map to state only: success = running/live, warning = degraded, danger = failed, info = queued, brand = plan/tier, neutral = everything else.
