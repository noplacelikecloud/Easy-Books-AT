One-line: user-owned metadata (labels, filters, regions) — removable, pill-shaped, never used for system state.

```jsx
<Tag onRemove={() => remove(id)}>eu-central-1</Tag>
```

Use Badge for state, Tag for user input. Tags sit in a flex row with gap 6-8px.
