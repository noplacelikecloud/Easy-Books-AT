One-line: one-line explanation for icon-only controls and truncated values.

```jsx
<Tooltip content="Letzter Build: vor 4 Min."><IconButton icon="info" label="Info" /></Tooltip>
```

Fades in over 140ms, no motion or delay animation. If the text needs two lines, it belongs in the UI instead.
