One-line: single-choice picker for known, short option sets (regions, plans, branches).

```jsx
<Select label="Region" options={["eu-central-1","eu-west-1","us-east-1"]} />
```

Matches Input geometry exactly. Chevron only — no double arrows, no custom popover.
