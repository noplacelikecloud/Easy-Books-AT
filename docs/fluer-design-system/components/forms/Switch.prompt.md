One-line: settings toggle that applies at once — label left, control right, full row width.

```jsx
<Switch label="Auto-Deploy" description="Bei jedem Push auf main." checked={on} onChange={setOn} />
```

Stack switches in a Card with 1px dividers between rows. Never use a Switch to submit a form.
