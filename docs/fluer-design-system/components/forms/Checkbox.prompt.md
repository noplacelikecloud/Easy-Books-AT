One-line: independent on/off options in forms and settings lists.

```jsx
<Checkbox label="Build-Cache nutzen" description="Beschleunigt Deployments um ca. 40 %." checked={on} onChange={...} />
```

3px radius box, teal fill when checked. Use Switch instead when the change applies immediately.
