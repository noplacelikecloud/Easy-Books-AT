# Copy into the Vercel project: easy-books-frontend
# (Dashboard → Settings → Environment Variables, or `vercel env add`)

# Production FastAPI URL (no trailing slash).
NEXT_PUBLIC_API_URL=https://easy-books-backend.vercel.app
