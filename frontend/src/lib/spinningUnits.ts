/** Re-export weight helpers — spinning uses the same Kg/Lbs/Bags convention as weaving. */
export {
  KG_TO_LB,
  weightTriple,
  ratePerLb,
  fmtWeight,
  formatWeightTriple,
  type WeightTriple,
} from "@/lib/weavingUnits"
